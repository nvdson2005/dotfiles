# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hyprlock config for =< 1080p monitor resolutions                                                        
# Original config submitted by https://github.com/SherLock707

# Sourcing colors generated by wallust
source = $HOME/.config/hypr/wallust/wallust-hyprland.conf
$Scripts = $HOME/.config/hypr/scripts

general {
    no_fade_in = true
    no_fade_out = true
    hide_cursor = false
    grace = 1 
    disable_loading_bar = true
}

background {
    monitor =
    # NOTE: use only 1 path
    #path = screenshot   # screenshot of your desktop
    #path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_modified   # NOTE only png supported for now
    #path = $HOME/.config/hypr/wallpaper_effects/VungTau2024.png 
    path = $HOME/Pictures/wallpapers/Kath.png
    #path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_current # current wallpaper
    
    #color = $color7

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    #blur_size = 5
    #blur_passes = 1 # 0 disables blurring
    #noise = 0.0117
    # contrast = 1.3000 # Vibrant!!!
    # brightness = 0.8000
    # vibrancy = 0.2100
    #vibrancy_darkness = 0.0
    # blur_passes = 2 #Really good blur effect. However, I prefer a background with no blur
    blur_passes = 0 
    contrast = 1
    brightness = 0.5
    vibrancy = 0.2
    vibrancy_darkness = 0.2
}

# input-field {
#     monitor =
#     size = 250, 50
#     outline_thickness = 3
#     dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
#     dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
#     dots_center = true
#     outer_color = $color5
#     inner_color = $color0
#     #font_color = $color12
#     font_color = rgba(0, 0, 0, .8)
#     fade_on_empty = true
#     placeholder_text = <i>Password...</i> # Text rendered in the input box when it's empty.
#     hide_input = false
#     
#     position = 0, 80
#     halign = center
#     valign = bottom
# }
input-field {
      monitor =
      size = 250, 60
      outline_thickness = 2
      dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
      dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
      dots_center = true
      outer_color = rgba(0, 0, 0, 0)
      inner_color = rgba(0, 0, 0, 0.2)
      font_color = rgba(255, 255, 255, 1)
      fade_on_empty = false
      rounding = -1
      check_color = rgb(62, 105, 221)
      placeholder_text = <i><span foreground="##cdd6f4">Type in your password...</span></i>
      hide_input = false
      position = 0, 80 
      halign = center
      valign = bottom
}
# # Date
# label {
#     monitor =
#     text = cmd[update:18000000] echo "<b> "$(date +'%A, %-d %B %Y')" </b>"
#     #color = $color12
#     color = rgba(169, 221, 255, 0.8)
#     font_size = 34
#     font_family = JetBrains Mono Nerd Font 10
#     position = 0, -80
#     halign = center
#     valign = top
# }
# DATE
label {
  monitor =
  text = cmd[update:1000] echo "$(date +"%A, %d %B")"
  color = rgba(242, 243, 244, 0.75)
  font_size = 22
  font_family = JetBrains Mono
  position = 0, -60
  halign = center 
  valign = top 
}
# TIME
label {
  monitor = 
  text = cmd[update:1000] echo "$(date +"%-I:%M %p")" #Hour:Minute AM/PM
  color = rgba(242, 243, 244, 0.75)
  font_size = 95
  font_family = JetBrains Mono Extrabold
  position = 0, 300
  halign = center
  valign = center
}
#Say Hi to the user
label {
    monitor =
    color = rgba(255, 255, 255, 1)
    text = What's up, Shon! 
    font_size = 15 
    font_family = JetBrains Mono Nerd Font Mono
    position = 0, 150
    halign = center
    valign = bottom
}

# # Hour-Time
# label {
#     monitor =
# #     text = cmd[update:1000] echo "$(date +"%H")"
#     text = cmd[update:1000] echo "$(date +"%I")" #AM/PM
#     #color = rgba(255, 255, 255, .6)
#     color = rgba(35, 148, 241, .6)
#     font_size = 150
#     font_family = JetBrains Mono Nerd Font Mono ExtraBold 
#     #position = 0, -200
#     position = -150, -350
#     halign = center
#     valign = top
# }
#
# # Minute-Time
# label {
#     monitor =
#     text = cmd[update:1000] echo "$(date +"%M")"
#     color = rgba(255, 255, 255, .6)
#     font_size = 150
#     font_family = JetBrains Mono Nerd Font Mono ExtraBold
#     position = 150, -350
#     halign = center
#     valign = top
# }
#
# # Seconds-Time
# label {
#     monitor =
# #     text = cmd[update:1000] echo "$(date +"%S")"
#      text = cmd[update:1000] echo "$(date +"%S %p")" #AM/PM
#      #color = $color12
#      color = rgba(255, 255, 255, .6)
#      font_size = 20
#      font_family = JetBrains Mono Nerd Font Mono ExtraBold
#      position = 325, -525
#      halign = center
#      valign = top
# }

# User
label {
    monitor =
    text =    $USER
    #color = $color10
    color = rgba(255, 255, 255, 1)
    font_size = 18
    font_family = Inter Display Medium

    position = 0, 20
    halign = center
    valign = bottom
}

# uptime
label {
    monitor =
    text = cmd[update:60000] echo "$(uptime -p || $Scripts/UptimeNixOS.sh)"
    #color = $color10
    color = rgba(255, 255, 255, 1)
    font_size = 18 
    font_family = JetBrains Mono 
    position = 0, 0
    halign = right
    valign = bottom
}

# # Battery
label {
    monitor =
    text = cmd[update:1000] /home/ducsonds2005/.config/hypr/UserScripts/battery.sh 
    color = rgba(242, 243, 244, 0.75)
    font_size = 15
    font_family = JetBrains Mono SemiBold
    position = 0, 0 
    halign = right 
    valign = top 
}

# Current Playing Song
label {
    monitor =
    text = cmd[update:1000] /home/ducsonds2005/.config/hypr/UserScripts/MyCurrentSong.sh 
    color = rgba(242, 243, 244, 0.75)
    #color = rgba(255, 255, 255, 0.6)
    font_size = 16
    font_family = Metropolis Light, Font Awesome 6 Free Solid
    position = 0, 0
    halign = left 
    valign = top 
}
# weather edit the scripts for locations
# weather scripts are located in ~/.config/hypr/UserScripts Weather.sh and/or Weather.py
# see https://github.com/JaKooLit/Hyprland-Dots/wiki/TIPS#%EF%B8%8F-weather-app-related-for-waybar-and-hyprlock
# The weather label is deprecated, because weather.com denies access from Viet Nam, and I'm lazy to set up also :>
# label {
#     monitor =
#     text = cmd[update:3600000] [ -f ~/.cache/.weather_cache ] && cat  ~/.cache/.weather_cache
#     #color = $color10
#     color = rgba(255, 255, 255 ,1)
#     font_size = 24
#     font_family = JetBrains Mono Nerd Font 10
#     position = 50, 0
#     halign = left
#     valign = bottom
# }

# Put a picture of choice here. Default is the current wallpaper
# image {
#     monitor =
#     path = $HOME/Pictures/profile-pictures/rounded.png
#     # path = $HOME/.config/hypr/wallpaper_effects/VungTau2024.png
#     size = 150
#     rounding = -1 
#     #Set to no border
#     border_size = 0 
#     # border_color = rgb(0, 0, 0) 
#     rotate = 0
#     reload_time = -1
#     position = 0, 120 
#     halign = center
#     valign = bottom
# }
